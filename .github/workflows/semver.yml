on:
  workflow_call:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: SemRelease
        uses: btnguyen2k/action-semrelease@v3.4.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
